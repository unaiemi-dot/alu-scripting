# Regular Expressions Project

This project contains Ruby scripts using Oniguruma regular expressions.
Each script matches patterns in strings provided as command-line arguments.

- `0-simply_match_school.rb` : Matches the word "School" in input strings.

